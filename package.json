{
  "name": "habibizz-kitchens",
  "private": true,
  "version": "1.0.0",
  "description": "Habibizz Kitchens - root package (frontend + backend are deployed separately)",
  "scripts": {
    "build": "echo 'No root build; deploy backend/ and frontend/ independently to Vercel.'"
  }
}
