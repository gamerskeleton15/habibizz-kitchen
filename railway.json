{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "command": "npm --prefix backend ci && npm --prefix backend run build"
  },
  "deploy": {
    "command": "npm --prefix backend start"
  }
}
